apiVersion: v1

kind: Service

metadata:
  name: "manual-cadvisor-metrics-service"
  namespace: "prod-blue-green"

spec:
   
  selector:
    app: "manual-cadvisor-metrics-label"

  ports:
  - protocol: TCP
    port:  80
    targetPort: 8080

  type: ClusterIP
    
