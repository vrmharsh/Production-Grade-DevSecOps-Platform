apiVersion: v1

kind: Service

metadata:
  name: "frontend-canary-service"
  namespace: "stage-canary"

spec:

  selector:
    app: "frontend-canary-label"

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
   

  type: ClusterIP 