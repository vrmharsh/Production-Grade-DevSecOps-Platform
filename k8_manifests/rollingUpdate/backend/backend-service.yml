apiVersion: v1
kind: Service

metadata:
  name: "backend-service"
  namespace: "dev-rolling-update"

spec:
   selector:
      app: backend-lb

   ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

   type: ClusterIP