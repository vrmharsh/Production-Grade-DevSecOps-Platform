apiVersion: v1
kind: Service

metadata:
  name: "frontend-service"
  namespace: "dev-rolling-update"

spec: 

  selector: 
    app: frontend-lb

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      
  type: ClusterIP